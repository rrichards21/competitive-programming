#include <bits/stdc++.h>
using namespace std;

int main(){
    int n,d,r;
    while(cin>>n>>d>>r,n != 0){
        vector<int> morn_routes(n);
        vector<int> even_routes(n);
        for(int i = 0; i < n; i++){
            cin>>morn_routes[i];
        }
        for(int i = 0; i < n; i++){
            cin>>even_routes[i];
        }

    }
    return 0;
}